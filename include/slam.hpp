#ifndef SLAM_HPP
#define SLAM_HPP

#include <Eigen/Dense>
#include <vector>

namespace Slam {
struct grid{
  Eigen::Vector4d mu;
  Eigen::Matrix<double, 4, 4> sigma;
}

class NDT {
public:

private:
  std
};

} // namespace Slam

#endif
